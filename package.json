{
  "name": "sloppers-monorepo",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.20.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "dev": "pnpm --filter @sloppers/server dev"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.13",
    "typescript": "^5.9.3"
  }
}
